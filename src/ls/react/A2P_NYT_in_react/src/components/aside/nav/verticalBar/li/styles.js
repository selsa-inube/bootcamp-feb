import styled from "styled-components";

const StyledLi = styled.li`
  margin: 20px;

  &:nth-child(2) a {
    font-size: 38px;
  }

  & a {
    color: #000;
    font-size: 18px;
    text-decoration: none;

    color: #000;
    font-size: 18px;
    text-decoration: none;
    &:hover {
      font-weight: bold;
    }
  }
`;

export { StyledLi };
